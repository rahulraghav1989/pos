<?php

namespace App\Http\Middleware;

use Closure;


class commonforusers
{
    /**
     * Handle an incoming request.
     *
     * @param  \Illuminate\Http\Request  $request
     * @param  \Closure  $next
     * @return mixed
     */
    //public function handle($request, Closure $next, $guard = null)
    //{
        //if (Auth::guard($guard)->check()) {
            

            
            //$request->merge(array('loggedinuser'=>$loggedinuser));
            //$request['loggedinuser']= $loggedinuser;

                /**
                 * This variable is available globally on all your views, and sub-views
                 */
                //view()->share('global_all_categories', $loggedinuser);
            //return $next($request);

       // }
        //return $next($request);
    //}
}
